fastapi==0.139.0
uvicorn[standard]==0.51.0
websockets==16.0
aiohttp==3.14.1
groq==1.5.0
edge-tts==7.2.8
python-dotenv==1.2.2
httpx==0.28.1
# torch: Docker installs the CPU-only wheel (torch==2.13.0+cpu) via the
# PyTorch index. For local dev, install manually:
#   pip install torch==2.13.0+cpu --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
numpy==2.5.1

fastapi==0.136.3
uvicorn[standard]==0.51.0
websockets==16.0
aiohttp==3.14.1
groq==1.4.0
edge-tts==7.2.8
python-dotenv==1.2.2
httpx==0.28.1
# torch: Docker installs the CPU-only wheel (torch==2.12.1+cpu) via the
# PyTorch index. For local dev, install manually:
#   pip install torch==2.12.1+cpu --index-url https://download.pytorch.org/whl/cpu
torch==2.12.1
numpy==2.4.4
